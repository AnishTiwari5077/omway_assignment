name: assignmet_omway
description: "MediCare Pharmacy Store - A Flutter Web App with Admin Panel"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router: ^17.3.0
  http: ^1.2.2
  provider: ^6.1.2
  flutter_animate: ^4.5.2
  google_fonts: ^8.1.0
  cached_network_image: ^3.4.1
  flutter_rating_bar: ^4.0.1
  intl: 0.20.2
  flutter_quill: ^11.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
